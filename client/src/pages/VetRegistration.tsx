import { useState } from "react";
import { AlertCircle, CheckCircle2, Loader2, Plus, Stethoscope, X } from "lucide-react";
import { Link, useLocation } from "wouter";
import { trpc } from "@/lib/trpc";
import { useAuth } from "@/_core/hooks/useAuth";
import { useLanguage } from "@/contexts/LanguageContext";
import { ProfessionalShell } from "@/components/ProfessionalShell";

export default function VetRegistration() {
  const { user } = useAuth();
  const { isArabic } = useLanguage();
  const [, setLocation] = useLocation();
  const [formData, setFormData] = useState({ licenseNumber: "", specializations: [] as string[], clinicName: "", clinicAddress: "", clinicPhone: "", clinicEmail: "", bio: "", consultationFee: "" });
  const [newSpecialization, setNewSpecialization] = useState("");
  const [error, setError] = useState("");
  const [success, setSuccess] = useState(false);
  const registerMutation = trpc.veterinarians.register.useMutation();
  const profileQuery = trpc.veterinarians.getProfile.useQuery(undefined, { enabled: Boolean(user) });

  const update = (field: string, value: string) => setFormData((current) => ({ ...current, [field]: value }));
  const addSpecialization = () => { const value = newSpecialization.trim(); if (!value) return; setFormData((current) => ({ ...current, specializations: [...current.specializations, value] })); setNewSpecialization(""); };
  const submit = async (event: React.FormEvent) => { event.preventDefault(); setError(""); if (!formData.licenseNumber || !formData.clinicName || !formData.clinicAddress) { setError(isArabic ? "أكمل رقم الترخيص واسم العيادة والعنوان." : "License number, clinic name, and address are required."); return; } try { await registerMutation.mutateAsync({ licenseNumber: formData.licenseNumber, specializations: formData.specializations, clinicName: formData.clinicName, clinicAddress: formData.clinicAddress, clinicPhone: formData.clinicPhone, clinicEmail: formData.clinicEmail, bio: formData.bio, consultationFee: formData.consultationFee ? parseFloat(formData.consultationFee) : undefined }); setSuccess(true); setTimeout(() => setLocation("/vet-dashboard"), 1800); } catch (err: any) { setError(err?.message || (isArabic ? "تعذر إنشاء الملف المهني." : "The professional profile could not be created.")); } };

  return <ProfessionalShell><section className="border-b border-slate-200 bg-white"><div className="mx-auto max-w-[1200px] px-5 py-12 lg:px-8"><div className="flex items-center gap-2 text-sm font-semibold text-[#0c5660]"><Stethoscope className="h-5 w-5" aria-hidden="true" />{isArabic ? "بوابة الطبيب البيطري" : "Veterinarian portal"}</div><h1 className="mt-4 max-w-3xl text-4xl font-semibold tracking-tight text-[#12343b] sm:text-5xl">{isArabic ? "أنشئ حضورك المهني على شبكة الرعاية" : "Create your professional presence in the care network"}</h1><p className="mt-5 max-w-3xl text-lg leading-8 text-slate-600">{isArabic ? "أضف بيانات الترخيص والعيادة والتخصصات لتقديم معلومات واضحة لأصحاب الحيوانات والتواصل ضمن مسار رعاية أكثر تنظيماً." : "Add your license, clinic, and specialties so pet owners can see clear information and reach you through a more organized care pathway."}</p></div></section><main className="mx-auto grid max-w-[1200px] gap-8 px-5 py-10 lg:grid-cols-[0.8fr_1.2fr] lg:px-8">{!user ? <section className="rounded-2xl border border-[#c5dfda] bg-[#edf7f5] p-7"><Stethoscope className="h-6 w-6 text-[#0c5660]" aria-hidden="true" /><h2 className="mt-4 text-2xl font-semibold text-[#12343b]">{isArabic ? "سجّل الدخول للمتابعة" : "Sign in to continue"}</h2><p className="mt-3 text-sm leading-6 text-slate-600">{isArabic ? "تحتاج إلى حساب موثق لإدارة ملفك المهني وطلبات العيادة." : "A verified account is required to manage your professional profile and clinic requests."}</p><Link href="/" className="mt-6 inline-flex rounded-lg bg-[#0c5660] px-4 py-2.5 text-sm font-semibold text-white">{isArabic ? "العودة إلى المنصة" : "Return to the platform"}</Link></section> : profileQuery.data ? <section className="rounded-2xl border border-[#b9dcd6] bg-white p-7 shadow-[0_8px_30px_rgba(15,23,42,0.04)]"><CheckCircle2 className="h-7 w-7 text-[#26704c]" aria-hidden="true" /><h2 className="mt-4 text-2xl font-semibold text-[#12343b]">{isArabic ? "ملفك المهني موجود بالفعل" : "Your professional profile already exists"}</h2><p className="mt-3 text-sm leading-6 text-slate-600">{isArabic ? "افتح لوحة الطبيب لإدارة معلوماتك وطلباتك." : "Open the veterinarian dashboard to manage your information and requests."}</p><Link href="/vet-dashboard" className="mt-6 inline-flex rounded-lg bg-[#0c5660] px-4 py-2.5 text-sm font-semibold text-white">{isArabic ? "فتح لوحة الطبيب" : "Open veterinarian dashboard"}</Link></section> : <><aside className="space-y-5"><div className="rounded-2xl border border-[#c5dfda] bg-[#edf7f5] p-6"><p className="text-xs font-semibold uppercase tracking-[0.14em] text-[#0c5660]">{isArabic ? "معلومات مهنية" : "Professional information"}</p><h2 className="mt-3 text-xl font-semibold text-[#12343b]">{isArabic ? "بيانات واضحة، تواصل آمن" : "Clear information, safer handoffs"}</h2><ul className="mt-5 space-y-3 text-sm leading-6 text-slate-600"><li className="flex gap-3"><CheckCircle2 className="mt-1 h-4 w-4 shrink-0 text-[#26704c]" aria-hidden="true" />{isArabic ? "رقم ترخيص وتخصصات يمكن مراجعتها" : "License and specialties that can be reviewed"}</li><li className="flex gap-3"><CheckCircle2 className="mt-1 h-4 w-4 shrink-0 text-[#26704c]" aria-hidden="true" />{isArabic ? "معلومات عيادة تساعد على الوصول" : "Clinic information that helps owners reach you"}</li><li className="flex gap-3"><CheckCircle2 className="mt-1 h-4 w-4 shrink-0 text-[#26704c]" aria-hidden="true" />{isArabic ? "ملف مهني لا يستبدل التحقق التنظيمي" : "A profile that does not replace regulatory verification"}</li></ul></div><div className="rounded-2xl border border-slate-200 bg-white p-6 text-sm leading-6 text-slate-600">{isArabic ? "لا تنشر بيانات شخصية غير ضرورية. استخدم معلومات العيادة المهنية فقط." : "Do not publish unnecessary personal information. Use professional clinic details only."}</div></aside><form onSubmit={submit} className="rounded-2xl border border-slate-200 bg-white p-6 shadow-[0_8px_30px_rgba(15,23,42,0.04)] sm:p-8"><div className="flex items-start justify-between border-b border-slate-100 pb-5"><div><p className="text-xs font-semibold uppercase tracking-[0.14em] text-[#0c5660]">{isArabic ? "التسجيل" : "Registration"}</p><h2 className="mt-2 text-2xl font-semibold text-[#12343b]">{isArabic ? "البيانات الأساسية" : "Core credentials"}</h2></div></div>{error && <div className="mt-5 flex gap-3 rounded-xl border border-red-200 bg-red-50 p-4 text-sm text-red-800"><AlertCircle className="h-5 w-5 shrink-0" aria-hidden="true" />{error}</div>}{success && <div className="mt-5 flex gap-3 rounded-xl border border-green-200 bg-green-50 p-4 text-sm text-green-800"><CheckCircle2 className="h-5 w-5 shrink-0" aria-hidden="true" />{isArabic ? "تم إنشاء الملف. جاري فتح لوحة الطبيب..." : "Profile created. Opening the veterinarian dashboard..."}</div>}<div className="mt-6 space-y-6"><label className="block"><span className="text-sm font-semibold text-slate-800">{isArabic ? "رقم الترخيص *" : "License number *"}</span><input required value={formData.licenseNumber} onChange={(event) => update("licenseNumber", event.target.value)} className="mt-2 w-full rounded-xl border border-slate-200 bg-slate-50 px-4 py-3 text-sm outline-none focus:border-[#7eb9b0]" placeholder="VET-2024-001234" /></label><div><span className="text-sm font-semibold text-slate-800">{isArabic ? "التخصصات" : "Specialties"}</span><div className="mt-2 flex gap-2"><input value={newSpecialization} onChange={(event) => setNewSpecialization(event.target.value)} onKeyDown={(event) => { if (event.key === "Enter") { event.preventDefault(); addSpecialization(); } }} className="min-w-0 flex-1 rounded-xl border border-slate-200 bg-slate-50 px-4 py-3 text-sm outline-none focus:border-[#7eb9b0]" placeholder={isArabic ? "أضف تخصصاً" : "Add a specialty"} /><button type="button" onClick={addSpecialization} className="inline-flex items-center gap-2 rounded-xl border border-slate-200 px-3 text-sm font-semibold text-slate-700 hover:border-[#86bdb5]"><Plus className="h-4 w-4" aria-hidden="true" />{isArabic ? "إضافة" : "Add"}</button></div><div className="mt-3 flex flex-wrap gap-2">{formData.specializations.map((item, index) => <span key={`${item}-${index}`} className="inline-flex items-center gap-2 rounded-full bg-[#edf7f5] px-3 py-1.5 text-xs font-semibold text-[#0c5660]">{item}<button type="button" onClick={() => setFormData((current) => ({ ...current, specializations: current.specializations.filter((_, itemIndex) => itemIndex !== index) }))} aria-label={isArabic ? "حذف التخصص" : "Remove specialty"}><X className="h-3.5 w-3.5" aria-hidden="true" /></button></span>)}</div></div><label className="block"><span className="text-sm font-semibold text-slate-800">{isArabic ? "اسم العيادة *" : "Clinic name *"}</span><input required value={formData.clinicName} onChange={(event) => update("clinicName", event.target.value)} className="mt-2 w-full rounded-xl border border-slate-200 bg-slate-50 px-4 py-3 text-sm outline-none focus:border-[#7eb9b0]" /></label><label className="block"><span className="text-sm font-semibold text-slate-800">{isArabic ? "عنوان العيادة *" : "Clinic address *"}</span><input required value={formData.clinicAddress} onChange={(event) => update("clinicAddress", event.target.value)} className="mt-2 w-full rounded-xl border border-slate-200 bg-slate-50 px-4 py-3 text-sm outline-none focus:border-[#7eb9b0]" /></label><div className="grid gap-4 sm:grid-cols-2"><label className="block"><span className="text-sm font-semibold text-slate-800">{isArabic ? "الهاتف" : "Phone"}</span><input value={formData.clinicPhone} onChange={(event) => update("clinicPhone", event.target.value)} className="mt-2 w-full rounded-xl border border-slate-200 bg-slate-50 px-4 py-3 text-sm outline-none focus:border-[#7eb9b0]" /></label><label className="block"><span className="text-sm font-semibold text-slate-800">{isArabic ? "البريد" : "Email"}</span><input type="email" value={formData.clinicEmail} onChange={(event) => update("clinicEmail", event.target.value)} className="mt-2 w-full rounded-xl border border-slate-200 bg-slate-50 px-4 py-3 text-sm outline-none focus:border-[#7eb9b0]" /></label></div><label className="block"><span className="text-sm font-semibold text-slate-800">{isArabic ? "النبذة المهنية" : "Professional bio"}</span><textarea value={formData.bio} onChange={(event) => update("bio", event.target.value)} className="mt-2 min-h-28 w-full rounded-xl border border-slate-200 bg-slate-50 px-4 py-3 text-sm outline-none focus:border-[#7eb9b0]" /></label><button type="submit" disabled={registerMutation.isPending || success} className="inline-flex w-full items-center justify-center gap-2 rounded-xl bg-[#0c5660] px-5 py-3 text-sm font-semibold text-white hover:bg-[#08434b] disabled:opacity-50">{registerMutation.isPending ? <><Loader2 className="h-4 w-4 animate-spin" aria-hidden="true" />{isArabic ? "جاري الحفظ..." : "Saving..."}</> : isArabic ? "إرسال الملف المهني" : "Submit professional profile"}</button></div></form></>}</main></ProfessionalShell>;
}
